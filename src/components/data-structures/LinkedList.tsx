import React from 'react';

/*
* Sequence:
*
* 1. The head of the linked list "Jane" fades in
* 2. A new node is added, "John" - to show add() method
* 3. A new node is added, "Jake"
* 4. A new node is added "Alice"
* 5. A new node is added "Bob"
* 6. A new node is added "Tim"
* 7. "John" is retrieved - to show getFirst() method
* 8. "Bob" is retrieved - to show get() method
* 9. "John" is removed - to show removeFirst() method
* 10. "Bob" is removed - to show remove() method
*
* */
function LinkedList() {

    return (
        <div className="container mx-auto mt-12 px-4 h-96">

        </div>
    );
}

export default LinkedList;